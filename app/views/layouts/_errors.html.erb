<% if object.errors.any? %>
  <div class="alert alert-danger mb10 mt25 ptb5">
    <a class="close" data-dismiss="alert">&times;</a>
    <h5><%= pluralize(object.errors.count, "error") %> prohibited this <%= object.class.name %> from being saved:</h5>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
